services:
  - type: web
    name: securehire-api
    env: docker
    dockerfilePath: ./Dockerfile
    dockerContext: .
    plan: free
    autoDeploy: true
    healthCheckPath: /health
    startCommand: uvicorn main:app --host 0.0.0.0 --port 8000